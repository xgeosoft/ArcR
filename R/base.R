#basics
standard_deviation = function(x,for.sample = TRUE){
  cat("\n=== Well ===\n")
  if(for.sample) return(sd(x))
  else return(sqrt(var(x)))
}


standard_deviation = function(x,for.sample = TRUE){
  cat("\n=== Well ===\n")
  if(for.sample) return(sd(x))
  else return(sqrt(var(x)))
}
